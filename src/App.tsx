import {Logo, Input, Button} from './components/elements';

const App = () => {
  return (
    <div>
      { <Logo /> }
      { <Input /> }
      { <Button />}
    </div>
  );
};

export default App;
